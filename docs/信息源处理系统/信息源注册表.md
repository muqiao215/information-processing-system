---
doc_type: reference
entity: Ductor 信息源注册表
status: active
owner: Muqiao
last_verified: 2026-04-20
---

# 信息源注册表

## 注册表口径

每一行代表一个可维护的信息源、采集编排层，或信息处理链路。

关键字段说明：

- `source_of_truth`: 真正可信的上游。
- `access_path`: 本机访问这个上游的工具或脚本。
- `acquisition_recipe`: 公开网页 / X / 单链接内容进入编排层时采用的规则集。
- `adapter_set`: recipe 允许调用的 adapter 集合。
- `ledger`: 每次 acquisition run 的尝试、结果、停止原因和提升决定。
- `contract`: 这个任务承诺产出什么。
- `failure_behavior`: 上游失败时应该如何处理。

## Active Sources

| source_id | card | 层级 | source_of_truth | access_path | owner / 承载 | schedule | contract | canonical outputs | failure_behavior |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `daily-news-summary` | [[Sources/daily-news-summary]] | collector + synthesizer | Claude Code / Codex CLI GitHub releases、橘鸦 AI 早报正文、国际 AI / developer-tool 正文、阮一峰周刊 | GitHub release 页面、正文页读取、必要时网页 reader | cron `daily-news-summary-7am` | `07:20 Asia/Shanghai` | 生成五段式中文晨报 | cron final report | 正文不可得时明确写 `正文不可得` 或 `细节不足`，不从标题硬编 |
| `github-trending-ai-watch` | [[Sources/github-trending-ai-watch]] | collector + selector | GitHub Trending daily page | `cron_tasks/daily-github-trending-ai-watch/scripts/fetch_github_trending.py` | cron `daily-github-trending-ai-watch` | `09:30 Asia/Shanghai` | Top 8 + 最值得看的 4 个 AI / agent / dev-tool 项目 | cron final report, `/tmp/github_trending_daily.json` | 抓取失败则停止并说明，不伪造榜单 |
| `follow-builders` | [[Sources/follow-builders]] | collector + selector + synthesizer | builders 聚合 feed，内部混合 X / podcast / blog | `cron_tasks/ai-builders-digest-5briefs/scripts/fetch_follow_builders.py` | skill `follow-builders` + cron `ai-builders-digest-5briefs` | `09:40 Asia/Shanghai` | A-E 五栏 AI Builders Digest + 下游 source manifest | `/root/.ductor/workspace/output_to_user/ai_builders_digest_latest.md`, `/root/.ductor/workspace/output_to_user/ai_builders_digest_sources_latest.json` | feed 加载失败时在报告里明确说明，不调用外部消息工具 |
| `builderpulse-opportunity-radar` | [[Sources/builderpulse-opportunity-radar]] | collector + selector + synthesizer | GitHub repo `BuilderPulse/BuilderPulse` 的每日 markdown archive | `git pull --ff-only` + `cron_tasks/daily-builderpulse-opportunity-radar/scripts/build_builderpulse_radar.py` | independent repo `/root/.ductor/workspace/vendor/BuilderPulse` + cron `daily-builderpulse-opportunity-radar` | `10:10 Asia/Shanghai` | 单独生成 `F. 机会雷达` | `/root/.ductor/workspace/output_to_user/builderpulse_opportunity_radar_latest.md`, `/root/.ductor/workspace/output_to_user/builderpulse_opportunity_radar_sources_latest.json` | pull 失败时报告失败，但继续使用本地最新 snapshot |
| `arxiv-llm-memory-discovery` | [[Sources/arxiv-llm-memory-discovery]] | collector + selector | arXiv API | `/root/.ductor/workspace/skills/arxiv-llm-memory-discovery/scripts/discover_llm_memory_paper.py` | skill `arxiv-llm-memory-discovery` + cron `daily-arxiv-llm-memory-discovery` | `09:00 Asia/Shanghai` | 每天最多 1 篇 LLM memory 高信号论文 | `/root/.ductor/workspace/output_to_user/arxiv_llm_memory_discovery_latest.md`, `/root/.ductor/workspace/output_to_user/arxiv_llm_memory_discovery_latest.json`, `/root/.ductor/workspace/output_to_user/arxiv_llm_memory_discovery_state.json` | 无高置信命中则返回空结果消息；arXiv rate limit 时 graceful skip |

## Downstream Consumers

| consumer_id | card | 层级 | input_source | access_path | owner / 承载 | schedule | contract | outputs | failure_behavior |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `notebooklm-report` | [[Consumers/notebooklm-report]] | artifact generator | `/root/.ductor/workspace/output_to_user/knowledge_pack_latest.json` | NotebookLM CLI + Chrome CDP `127.0.0.1:9222` | cron `daily-notebooklm-content-gen` | `10:20 Asia/Shanghai` | 根据 canonical knowledge pack 生成 NotebookLM briefing report | `/root/.ductor/workspace/output_to_user/notebooklm_report_YYYYMMDD.md`, `/root/.ductor/workspace/output_to_user/notebooklm_report_run_latest.json` | CDP、auth 或 pack 缺失时停止并报告阻塞点；可一次性退回 legacy builders manifest；URL 导入失败时可写 fallback markdown source |
| `notebooklm-artifact-trigger` | [[Consumers/notebooklm-artifact-trigger]] | artifact generator | `/root/.ductor/workspace/output_to_user/notebooklm_report_run_latest.json` | NotebookLM CLI + Chrome CDP `127.0.0.1:9222` | cron `daily-notebooklm-artifact-trigger` | `10:35 Asia/Shanghai` | 基于 report run metadata 触发 slide / video artifact | NotebookLM artifact metadata | 生成请求被接受并返回 artifact id 才算成功；未就绪按 async pending 处理 |

## Candidate Adapters

| adapter_id | card | role | keep | avoid |
| --- | --- | --- | --- | --- |
| `acquisition-orchestrator` | [[Adapters/acquisition-orchestrator]] | source collector 与 `knowledge_pack` 之间的本地采集编排层；负责 recipe 选择、adapter 调用、ledger 写入、promotion 决策 | 本地优先、no-extra-key 默认、结构化 ledger、把抓取行为从 source contract 中解耦 | 把它登记成 active source，或把 NotebookLM 逻辑塞回 acquisition 层 |
| `firecrawl-web-agent-fetch` | [[Adapters/firecrawl-web-agent-fetch]] | 可选的 agentic web acquisition adapter / 参考实现 | 借鉴 map/crawl/interact/extract 架构，作为显式 opt-in adapter | 当成默认依赖、默认要求 `FIRECRAWL_API_KEY`、把 Firecrawl 写成主 source |
| `qiaomu-anything-to-notebooklm` | [[Adapters/qiaomu-anything-to-notebooklm]] | 多源预处理 + NotebookLM 消化适配器 | 网页 / X 获取 cascade、付费墙 fallback、输入分流逻辑 | 需要额外 API key 的预处理路径、把它误当 active source |
| `notebooklm-source-pack` | [[Adapters/notebooklm-source-pack]] | 多个 source manifest 到 canonical `knowledge_pack` 的本地适配层；cron `daily-knowledge-pack-builder` 每天 `10:15 Asia/Shanghai` 运行 | 统一 schema、去重、fallback markdown、作为 digestion 唯一上游入口 | 继续让 NotebookLM cron 直接绑定单一 source manifest |

## Acquisition Recipes

这些 recipe 不单独算 active source。它们属于 acquisition orchestrator 的行为模板。

| recipe_id | applies_to | default adapters | optional adapters | promotion target | notes |
| --- | --- | --- | --- | --- | --- |
| `public-web-default` | 公开网页单链接 | `r.jina.ai` -> `defuddle.md` -> browser-like fetch -> AMP -> `archive.today` -> browser / agent fetch | `firecrawl-web-agent-fetch` | candidate extraction -> `knowledge_pack` item | 默认 no-extra-key，适合 blog / docs / news / landing page |
| `x-post-default` | `x.com` / `twitter.com` 单链接 | `r.jina.ai` -> `defuddle.md` -> browser-like fetch -> `archive.today` -> browser / agent fetch | `firecrawl-web-agent-fetch` | candidate extraction -> `knowledge_pack` item | feed 级采集仍由 source collector 负责，单贴深挖走 acquisition |
| `manifest-localize` | manifest 已含 `summary` / `raw_text` / `fallback_content` 的条目 | manifest text -> local markdown materialization | browser / optional Firecrawl only if text is insufficient | localized text -> `knowledge_pack` item | 作为无法稳定 live fetch 时的兜底 recipe |

## Pipeline Engines

这些不是 active source，也不是普通 skill。它们是后续重构本地信息流水线时可借鉴或承载的实现骨架。

| engine_id | card | role | borrow | avoid |
| --- | --- | --- | --- | --- |
| `auto-paper-digest-engine` | [[Pipelines/auto-paper-digest-engine]] | APD 风格 source object / 状态机 / NotebookLM artifact / publishing pipeline 参考实现 | 状态机、日期化 data 目录、artifact ledger、portal / publisher 分层 | 直接复制 NotebookLM Playwright bot、把 HF papers 当通用主源、把 Douyin/HF 发布绑进信息获取层 |

## Target Layer Split

后续目标不是继续扩展单张“信息源”表，而是拆成两层：

| 大层 | 包含 | 边界 |
| --- | --- | --- |
| 信息获取层 | collector, acquisition orchestrator, selector, knowledge pack | 输出标准 `knowledge_pack`，不创建 NotebookLM notebook |
| 知识消化层 | NotebookLM ingest, report, artifact, publishing | 消费标准 pack，不直接抓上游 source |

采集编排层内部建议继续拆成：

| 子层 | 职责 | 输出 |
| --- | --- | --- |
| source collectors | 产生原始条目、URL、正文或 manifest | raw entries / manifests |
| acquisition recipes | 决定不同 item type 的抓取策略与允许 adapter | recipe selection |
| acquisition adapters | 执行 reader / browser / crawl / extract 行为 | attempt results |
| acquisition ledger | 记录 attempts、selected result、promotion notes | run ledger JSON |
| promotion into `knowledge_pack` | 把稳定结构化结果转成 canonical items | `knowledge_pack` candidates / items |

已落地中间 cron：

- `daily-knowledge-pack-builder` at `10:15 Asia/Shanghai`

推荐长期目录：

- `/root/.ductor/workspace/output_to_user/information_pipeline/acquisition/YYYY-MM-DD/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/bundles/YYYY-MM-DD/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/digestion/YYYY-MM-DD/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/artifacts/YYYY-MM-DD/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/ledgers/YYYY-MM-DD.json`

建议 acquisition 侧至少再有：

- `/root/.ductor/workspace/output_to_user/information_pipeline/acquisition/YYYY-MM-DD/recipes/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/acquisition/YYYY-MM-DD/candidates/`
- `/root/.ductor/workspace/output_to_user/information_pipeline/ledgers/acquisition/YYYY-MM-DD/`

## Excluded Ops Telemetry

这些任务是状态监控，不属于内容信息源采集。

| cron ID | 原因 |
| --- | --- |
| `daily-cpa-minimax-glm-quota-check-9am` | provider / CPA 额度遥测 |
| `daily-cpa-minimax-glm-quota-check-12pm` | provider / CPA 额度遥测 |
| `daily-cpa-minimax-glm-quota-check-3pm` | provider / CPA 额度遥测 |
| `daily-cpa-minimax-glm-quota-check-6pm` | provider / CPA 额度遥测 |
| `daily-cpa-minimax-glm-quota-check-9pm` | provider / CPA 额度遥测 |
| `daily-new-api-health-check-v2` | new-api 服务健康检查 |
| `daily-new-api-midnight-health-check` | new-api 服务健康检查 |
| `daily-cpa-auth-pool-sync-710am` | CPA auth 池运维同步 |
| `daily-private-repo-refresh-and-skill-sync-7am` | 私有仓和 skill 同步 |
| `daily-codex-pool-check-racknerd-bf2025` | 已禁用的账号池检查 |

## 维护规则

1. 新增采集 cron 时，先补本表，再创建或修改任务。
2. 修改任务输出路径时，同步更新 `canonical outputs`。
3. 如果任务只是下游加工，不要放进 `Active Sources`，放进 `Downstream Consumers`。
4. 如果任务是额度、健康、账号池、同步类，不要放进内容信息源表，放进 `Excluded Ops Telemetry`。
5. 如果任务是 APD 这类流水线实现骨架，不要放进 `Active Sources`，放进 `Pipeline Engines`。
6. NotebookLM 相关消费者后续应优先读取 `knowledge_pack_latest.json`，而不是直接绑定某一个 source manifest。
7. 公开网页 / X 的抓取策略要登记为 acquisition recipe + adapter set，而不是只写单个 fallback_tool。
8. Firecrawl / web-agent 只能登记在 `Candidate Adapters`，不能登记到 `Active Sources`。
